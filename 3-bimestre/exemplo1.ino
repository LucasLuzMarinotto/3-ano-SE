float sensorCentro = A0; float sensorPontaD = A1; float sensorPontaE = A2; int estado1; int estado2; int estado3; int n1 = 6; int n2 = 3; int n3 = 5; int n4 = 9; //int pwm = 180; int nulo = 0; void setup() {    Serial.begin(9600);   pinMode(n1, OUTPUT);   pinMode(n2, OUTPUT);   pinMode(n3, OUTPUT);   pinMode(n4, OUTPUT); } void loop() {     estado1 = analogRead(sensorCentro);   estado2 = analogRead(sensorPontaD);   estado3 = analogRead(sensorPontaE);   Serial.print ("Centro ");   Serial.print (estado1);   Serial.print (" Direita ");   Serial.print (estado2);   Serial.print (" Esquerda ");   Serial.println (estado3);   delay(400);       if ((estado2 < 100) && (estado3 < 100) && (estado1 > 100)) {     frente();           }else if ((estado2 > 100) && (estado3 < 100) && (estado1 < 100)){       direita();           }else if ((estado2 < 100) && (estado3 > 100) && (estado1 < 100)){       esquerda();           }else if ((estado2 > 100) && (estado3 > 100) && (estado1 > 100)){       parar();           }else if ((estado2 > 100) && (estado3 < 100) && (estado1 > 100)){       frente();           }else if ((estado2 < 100) && (estado3 > 100) && (estado1 > 100)){       direita();           }else if ((estado2 < 100) && (estado3 > 100) && (estado1 < 100)){       esquerda();           }else if ((estado2 > 100) && (estado3 > 100) && (estado1 > 100)){       esquerda();         } }   void frente(){    analogWrite(n1, 1); //motor A para frente    analogWrite(n2, 0); //motor B para frente    analogWrite(n3, 1); // motor A para tras    analogWrite(n4, 0); //motor B para trás   }   void direita(){    analogWrite(n1, 0); //motor A para frente    analogWrite(n2, 1); //motor B para frente    analogWrite(n3, 0); // motor A para tras    analogWrite(n4, 1); //motor B para trás   }   void esquerda(){    analogWrite(n1, 1); //motor A para frente    analogWrite(n2, 0); //motor B para frente    analogWrite(n3, 0); // motor A para tras    analogWrite(n4, 1); //motor B para trás   }   void parar(){    analogWrite(n1, 0); //motor A para frente    analogWrite(n2, 0); //motor B para frente    analogWrite(n3, 0); // motor A para tras    analogWrite(n4, 0); //motor B para trás   }
